# Milestone 3 — Theming Story Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add elevation tokens end-to-end (Figma → repo → 9 rebound components), write and prove a repeatable "add a brand" runbook, and document the font fallback story — closing the last "Figma-first theming" gap in `polish-plan.md` Milestone 3.

**Architecture:** Three independent, separately-committable deliverables. Task 1 (elevation tokens) is purely repo-side — the Figma-side variables already exist live (created by the orchestrator this session via the Figma MCP; exact IDs and values are embedded verbatim below). Task 2 (add-brand runbook) is a new skill doc, proven by a live throwaway Figma round-trip the orchestrator runs directly (not a subagent task — see "Orchestrator-run verification" below). Task 3 (font strategy doc) is pure documentation, cross-checked against the actual source files it describes.

**Tech Stack:** Next.js 16 / Tailwind v4.3.1 / DTCG token JSON / Figma variables (via MCP) / the kit's own `npm run tokens:build` + `npm run manifests:build` + `npm run manifests:check` pipeline.

## Global Constraints

- Every color/spacing/radius/typography/shadow value binds a `--ds-*` token — no hex/px/raw-shadow literals in components. (`CLAUDE.md`)
- Generated files are never hand-edited: `manifests/**` (except `overrides/`), `docs/components/**`, `registry.json`, `src/styles/tokens.css`. Edit the source (`tokens/tokens.dtcg.json` + `tokens/tokens.raw.css`), then regenerate. (`CLAUDE.md`)
- `tokens/tokens.raw.css` and `tokens/tokens.dtcg.json` are themselves edited "via token-sync" per `CLAUDE.md`'s provenance table — they are the canonical _source_, not a generated artifact, but must always match what the live Figma file says.
- Second brand theme is named `brand` — its retired internal name is banned everywhere. (Already verified clean in Figma this session — 12 stale variables using the retired name were found and renamed; zero remain.)
- `npm run manifests:check` (schemas + byte-drift + cross-checks) must stay green after every task.
- Dark mode is out of scope for this milestone entirely (dropped from the original M3 scope — see `docs/superpowers/specs/2026-07-13-m3-theming-design.md` "Scope note").

---

### Task 1: Elevation tokens

**Files:**

- Modify: `tokens/tokens.dtcg.json` (insert `global.shadow` after line 9527, insert `semantic.shadow` after line 13461 — see exact line numbers/content below; re-check line numbers before editing, since earlier edits in this same task shift later line numbers)
- Modify: `tokens/tokens.raw.css` (insert 5 lines before the `[data-theme="global"]` block's closing `}` at line 305; insert 3 lines before `[data-theme="jit"]`'s closing `}` at line 557; insert the same 3 lines before `[data-theme="brand"]`'s closing `}` at line 809)
- Generate: `src/styles/tokens.css` (via `npm run tokens:build` — do not hand-edit)
- Modify: `src/components/ui/checkbox.tsx`
- Modify: `src/components/ui/dropdown-menu.tsx`
- Modify: `src/components/ui/hover-card.tsx`
- Modify: `src/components/ui/navigation-menu.tsx`
- Modify: `src/components/ui/popover.tsx`
- Modify: `src/components/ui/select.tsx`
- Modify: `src/components/ui/sonner.tsx`
- Modify: `src/components/ui/alert-dialog.tsx`
- Modify: `src/components/ui/drawer.tsx`
- Generate: `manifests/**`, `docs/components/**` (via `npm run manifests:build` — do not hand-edit)

**Interfaces:**

- Produces: 5 new global CSS custom properties (`--ds-shadow-xs`, `--ds-shadow-sm`, `--ds-shadow-md`, `--ds-shadow-lg`, `--ds-shadow-xl`) and 3 new semantic CSS custom properties (`--ds-shadow-control`, `--ds-shadow-overlay`, `--ds-shadow-modal`), available to every later task and to any component via `[box-shadow:var(--ds-shadow-<role>)]`.

**Figma-side data (already live — created this session via the Figma MCP against JIT DS 2.1, `Y3gNgjmXe1t67fPlDjM2iH`; do not recreate, only mirror into the repo):**

| Figma variable                                                              | ID                  | Type         | Value / alias                                                         |
| --------------------------------------------------------------------------- | ------------------- | ------------ | --------------------------------------------------------------------- |
| `shadow/xs` (global collection, mode `global` = `68:0`)                     | `VariableID:4026:2` | STRING       | `0 1px 2px 0 rgb(0 0 0 / 0.05)`                                       |
| `shadow/sm`                                                                 | `VariableID:4026:3` | STRING       | `0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1)`       |
| `shadow/md`                                                                 | `VariableID:4026:4` | STRING       | `0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1)`    |
| `shadow/lg`                                                                 | `VariableID:4026:5` | STRING       | `0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1)`  |
| `shadow/xl`                                                                 | `VariableID:4026:6` | STRING       | `0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1)` |
| `shadow/control` (semantic collection, modes `jit`=`68:1`, `brand`=`746:1`) | `VariableID:4026:7` | STRING alias | → `shadow/sm` in both modes                                           |
| `shadow/overlay`                                                            | `VariableID:4026:8` | STRING alias | → `shadow/md` in both modes                                           |
| `shadow/modal`                                                              | `VariableID:4026:9` | STRING alias | → `shadow/lg` in both modes                                           |

Global collection ID: `VariableCollectionId:68:262`. Semantic collection ID: `VariableCollectionId:68:263`.

- [ ] **Step 1: Insert `global.shadow` into `tokens/tokens.dtcg.json`**

Open `tokens/tokens.dtcg.json`. Find the line `    },` that closes `"lineHeight": { ... }` immediately followed by `    "spacing": {` (this was line 9527/9528 when last checked — re-locate by searching for `"lineHeight"` under the `global` key, since it's the 7th of 8 keys in `global`, alphabetically right before `spacing`). Insert the following block immediately after that closing `    },` line, before `    "spacing": {`:

```json
    "shadow": {
      "xs": {
        "$description": "xs — Global shadow step — referenced by semantic/shadow tokens, never applied directly.",
        "$extensions": {
          "figma-console-mcp": {
            "collectionId": "VariableCollectionId:68:262",
            "lastSyncedAt": "2026-07-13T12:00:00.000Z",
            "lastSyncedValue": {
              "global": {
                "literal": "0 1px 2px 0 rgb(0 0 0 / 0.05)"
              }
            },
            "variableId": "VariableID:4026:2"
          }
        },
        "$type": "string",
        "$value": "0 1px 2px 0 rgb(0 0 0 / 0.05)"
      },
      "sm": {
        "$description": "sm — Global shadow step — referenced by semantic/shadow tokens, never applied directly.",
        "$extensions": {
          "figma-console-mcp": {
            "collectionId": "VariableCollectionId:68:262",
            "lastSyncedAt": "2026-07-13T12:00:00.000Z",
            "lastSyncedValue": {
              "global": {
                "literal": "0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1)"
              }
            },
            "variableId": "VariableID:4026:3"
          }
        },
        "$type": "string",
        "$value": "0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1)"
      },
      "md": {
        "$description": "md — Global shadow step — referenced by semantic/shadow tokens, never applied directly.",
        "$extensions": {
          "figma-console-mcp": {
            "collectionId": "VariableCollectionId:68:262",
            "lastSyncedAt": "2026-07-13T12:00:00.000Z",
            "lastSyncedValue": {
              "global": {
                "literal": "0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1)"
              }
            },
            "variableId": "VariableID:4026:4"
          }
        },
        "$type": "string",
        "$value": "0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1)"
      },
      "lg": {
        "$description": "lg — Global shadow step — referenced by semantic/shadow tokens, never applied directly.",
        "$extensions": {
          "figma-console-mcp": {
            "collectionId": "VariableCollectionId:68:262",
            "lastSyncedAt": "2026-07-13T12:00:00.000Z",
            "lastSyncedValue": {
              "global": {
                "literal": "0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1)"
              }
            },
            "variableId": "VariableID:4026:5"
          }
        },
        "$type": "string",
        "$value": "0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1)"
      },
      "xl": {
        "$description": "xl — Global shadow step — referenced by semantic/shadow tokens, never applied directly.",
        "$extensions": {
          "figma-console-mcp": {
            "collectionId": "VariableCollectionId:68:262",
            "lastSyncedAt": "2026-07-13T12:00:00.000Z",
            "lastSyncedValue": {
              "global": {
                "literal": "0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1)"
              }
            },
            "variableId": "VariableID:4026:6"
          }
        },
        "$type": "string",
        "$value": "0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1)"
      }
    },
```

- [ ] **Step 2: Insert `semantic.shadow` into `tokens/tokens.dtcg.json`**

Find the line `    },` that closes `"radius": { ... }` immediately followed by `    "spacing": {` under the `semantic` key (`radius` is the 4th of 6 keys in `semantic`, alphabetically right before `spacing`). Insert the following block immediately after that closing `    },` line, before `    "spacing": {`:

```json
    "shadow": {
      "control": {
        "$description": "Elevation for form controls (e.g. checkbox). Both brands: shadow.sm.",
        "$extensions": {
          "figma-console-mcp": {
            "collectionId": "VariableCollectionId:68:263",
            "lastSyncedAt": "2026-07-13T12:00:00.000Z",
            "lastSyncedValue": {
              "jit": {
                "reference": "{shadow.sm}"
              },
              "brand": {
                "reference": "{shadow.sm}"
              }
            },
            "variableId": "VariableID:4026:7"
          },
          "modes": {
            "brand": "{shadow.sm}"
          }
        },
        "$type": "string",
        "$value": "{shadow.sm}"
      },
      "overlay": {
        "$description": "Elevation for floating transient content (menus, popovers, toasts). Both brands: shadow.md.",
        "$extensions": {
          "figma-console-mcp": {
            "collectionId": "VariableCollectionId:68:263",
            "lastSyncedAt": "2026-07-13T12:00:00.000Z",
            "lastSyncedValue": {
              "jit": {
                "reference": "{shadow.md}"
              },
              "brand": {
                "reference": "{shadow.md}"
              }
            },
            "variableId": "VariableID:4026:8"
          },
          "modes": {
            "brand": "{shadow.md}"
          }
        },
        "$type": "string",
        "$value": "{shadow.md}"
      },
      "modal": {
        "$description": "Elevation for blocking modal surfaces (dialogs, drawers). Both brands: shadow.lg.",
        "$extensions": {
          "figma-console-mcp": {
            "collectionId": "VariableCollectionId:68:263",
            "lastSyncedAt": "2026-07-13T12:00:00.000Z",
            "lastSyncedValue": {
              "jit": {
                "reference": "{shadow.lg}"
              },
              "brand": {
                "reference": "{shadow.lg}"
              }
            },
            "variableId": "VariableID:4026:9"
          },
          "modes": {
            "brand": "{shadow.lg}"
          }
        },
        "$type": "string",
        "$value": "{shadow.lg}"
      }
    },
```

- [ ] **Step 3: Validate the JSON**

Run: `node -e "JSON.parse(require('fs').readFileSync('tokens/tokens.dtcg.json','utf8')); console.log('valid')"`
Expected: `valid` (if this fails, the insertion broke JSON syntax — check for a missing/extra comma at the insertion boundary).

- [ ] **Step 4: Insert into `tokens/tokens.raw.css`**

In the `[data-theme="global"]` block (starts at line 2), insert these 5 lines immediately before the block's closing `}` (currently line 305):

```css
--ds-shadow-xs: 0 1px 2px 0 rgb(0 0 0 / 0.05);
--ds-shadow-sm: 0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1);
--ds-shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);
--ds-shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
--ds-shadow-xl: 0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1);
```

In the `[data-theme="jit"]` block, insert these 3 lines immediately before the block's closing `}` (currently line 557):

```css
--ds-shadow-control: var(--ds-shadow-sm);
--ds-shadow-overlay: var(--ds-shadow-md);
--ds-shadow-modal: var(--ds-shadow-lg);
```

In the `[data-theme="brand"]` block, insert the identical 3 lines immediately before the block's closing `}` (currently line 809):

```css
--ds-shadow-control: var(--ds-shadow-sm);
--ds-shadow-overlay: var(--ds-shadow-md);
--ds-shadow-modal: var(--ds-shadow-lg);
```

Do not add anything to the `[data-theme="component"]` block — no component-layer shadow tokens exist; components bind the semantic tokens directly.

- [ ] **Step 5: Rebuild `src/styles/tokens.css`**

Run: `npm run tokens:build`
Expected: `✓ Built src/styles/tokens.css from 4 source blocks` (the count reflects the 4 `[data-theme]` blocks, unchanged — this run just picks up the new properties inside them). Confirm the new properties landed:

Run: `grep -c -- "--ds-shadow-" src/styles/tokens.css`
Expected: `11` (5 global + 3×2 semantic entries across the `:root, [data-theme="jit"]` and `[data-theme="brand"]` blocks — the global 5 appear once under `:root`, the semantic 3 appear once per brand block = 5 + 3 + 3 = 11).

- [ ] **Step 6: Rebind `checkbox.tsx`**

Read `src/components/ui/checkbox.tsx`, find the `shadow-sm` Tailwind class in its `cva()` call, and replace it with `[box-shadow:var(--ds-shadow-control)]`.

- [ ] **Step 7: Rebind the 6 `shadow-md` files**

In each of `src/components/ui/dropdown-menu.tsx`, `src/components/ui/hover-card.tsx`, `src/components/ui/navigation-menu.tsx`, `src/components/ui/popover.tsx`, `src/components/ui/select.tsx`, `src/components/ui/sonner.tsx`: find the `shadow-md` Tailwind class and replace it with `[box-shadow:var(--ds-shadow-overlay)]`.

- [ ] **Step 8: Rebind the 2 `shadow-lg` files**

In each of `src/components/ui/alert-dialog.tsx`, `src/components/ui/drawer.tsx`: find the `shadow-lg` Tailwind class and replace it with `[box-shadow:var(--ds-shadow-modal)]`.

- [ ] **Step 9: Confirm no raw shadow classes remain**

Run: `grep -rln -E 'shadow-(sm|md|lg|xl|2xl)' src/components/ui/*.tsx`
Expected: no output (empty — all 9 occurrences rebound; if any file still matches, that rebind was missed).

- [ ] **Step 10: Rebuild manifests and validate**

Run: `npm run manifests:build && npm run manifests:check`
Expected: both commands exit 0. `manifests:check` includes the byte-drift gate — if it fails, re-run `npm run manifests:build` and check `git diff` for anything unexpected before re-running `manifests:check`.

- [ ] **Step 11: Type-check and build**

Run: `npx tsc --noEmit && npm run build`
Expected: both exit 0 (confirms the `[box-shadow:var(...)]` arbitrary-value syntax compiles under Tailwind v4).

- [ ] **Step 12: Visual verification (zero regression)**

Run: `npm run storybook` (or `npm run dev` for the showcase). Open the stories/pages for all 9 rebound components (`Checkbox`, `DropdownMenu`, `HoverCard`, `NavigationMenu`, `Popover`, `Select`, `Sonner`/toast, `AlertDialog`, `Drawer`). Confirm each renders with the same shadow depth as before this task (the values are byte-identical to Tailwind's own `shadow-sm/md/lg`, so there must be zero visible difference). Toggle the brand switcher on at least one overlay component (e.g. `Popover`) and confirm the shadow is present and unchanged in both `jit` and `brand`.

- [ ] **Step 13: Commit**

```bash
git add tokens/tokens.dtcg.json tokens/tokens.raw.css src/styles/tokens.css \
  src/components/ui/checkbox.tsx src/components/ui/dropdown-menu.tsx \
  src/components/ui/hover-card.tsx src/components/ui/navigation-menu.tsx \
  src/components/ui/popover.tsx src/components/ui/select.tsx src/components/ui/sonner.tsx \
  src/components/ui/alert-dialog.tsx src/components/ui/drawer.tsx \
  manifests/ docs/components/
git commit -m "feat(tokens): add --ds-shadow-* elevation tokens, rebind 9 components"
```

---

### Task 2: Add-brand runbook

**Files:**

- Create: `.claude/skills/add-brand/SKILL.md`

**Interfaces:**

- Consumes: nothing from Task 1 (independent).
- Produces: a skill doc referenced by README (wired in Task 3) and by `polish-plan.md` at milestone close (done by the orchestrator, not this task).

**Reference file:** `.claude/skills/token-sync/SKILL.md` — read it first; the new skill must match its structure (frontmatter, numbered steps, a "Done when" section) and tone.

- [ ] **Step 1: Write the skill file**

Create `.claude/skills/add-brand/SKILL.md` with this exact content:

```markdown
---
name: add-brand
description: Onboard a new brand theme into the kit by adding a mode to the Figma semantic collection and syncing it through the token pipeline. Trigger when asked to "add a brand", "add a new theme", or "onboard a client brand".
---

# add-brand — onboard a new brand theme

The ritual for adding a third (or Nth) brand to the kit. Themes are authored
**in Figma, not in code** — this skill is the bridge from a new Figma mode to
a working `data-theme="<brand>"` in the running app, with zero component
code changes.

## Preconditions

- Figma desktop app open on **JIT DS 2.1** (`Y3gNgjmXe1t67fPlDjM2iH`) with the
  Figma Console MCP desktop bridge running. Check with `figma_get_status`
  (probe). If the bridge is down, stop and ask the user to open Figma + the
  bridge — step 1 cannot run without it.
- Know the new brand's name up front (kebab-case, e.g. `acme`) — this becomes
  the `data-theme` value everywhere. Never reuse the second brand's retired
  internal name — confirm the exact naming constraint with the user first.

## Step 1 — Add a mode in Figma

In the `semantic` variable collection (the _only_ layer that varies per
brand — `global` and `component` never change per brand), add a new mode
named after the brand. Figma requires a value for every semantic variable in
the new mode — copy from an existing mode (`jit` or `brand`) as a starting
point, then adjust colors/radius/typography/shadow values for the new brand's
identity. Do not touch the `global` or `component` collections — a new brand
should only ever add a `semantic` mode.

## Step 2 — Sync

Run the `token-sync` skill (Steps 1–2 of `.claude/skills/token-sync/SKILL.md`):
export the three real collections (`global`, `semantic`, `component`) via
`figma_export_tokens`, which pulls the new mode into
`tokens/tokens.dtcg.json` + `tokens/tokens.raw.css`, then `npm run tokens:build`
to regenerate `src/styles/tokens.css`.

## Step 3 — Verify

- `npm run build` (or `tsc --noEmit`) passes.
- Set `data-theme="<new-brand>"` on `<html>` (or use the Storybook/showcase
  brand toggle) and confirm the kit re-skins with **zero component-file
  changes** — every component already binds `--ds-*` semantic tokens, so a
  new semantic mode is automatically picked up.
- Run `npm run manifests:check` — component tokens (`--ds-*-fill`, …) must
  **not** have changed. Only semantic values should differ per brand; if a
  component token changed, something was added to the wrong collection.

## Done when

The new mode renders correctly via the brand toggle, `manifests:check`
passes, and no `.tsx` file under `src/components/ui/` was touched. Commit all
three token files (`tokens.dtcg.json`, `tokens.raw.css`, `tokens.css`)
together, same as `token-sync`.
```

- [ ] **Step 2: Commit**

```bash
git add .claude/skills/add-brand/SKILL.md
git commit -m "docs(skill): add add-brand runbook for onboarding new brand themes"
```

---

### Task 3: Font strategy doc

**Files:**

- Create: `docs/font-strategy.md`
- Modify: `README.md` (link the new doc from "Creating a new theme"; flip the "Figma-first theming" status line from ⬜ to ✅)

**Interfaces:**

- Consumes: nothing from Tasks 1–2 (independent, but sequenced last since it references the add-brand skill by name).

**Facts verified this session (cite these exactly — do not invent alternatives):**

- `src/styles/kraken-theme.css:58` — `--font-sans: var(--ds-typography-bodymd-fontfamily), ui-sans-serif, system-ui, -apple-system, sans-serif;`
- `src/app/globals.css:17-19` — comment: `Brand fonts: "Noto Sans" (Brand) is loaded via a <link> in layout.tsx; "Moderat JIT" (JIT, proprietary) renders from the OS if installed — add woff2 to public/fonts/ + an @font-face here to make it portable.`
- `src/app/layout.tsx:32-37` — Noto Sans is loaded via Google Fonts `<link>` tags (`fonts.googleapis.com/css2?family=Noto+Sans:...`), registered under its literal name so the `--ds-typography-bodymd-fontfamily` token resolves to it.
- `tokens/tokens.dtcg.json` `global.fontFamily` — `jit` → `"Moderat JIT"`, `brand` → `"Noto Sans"` (both STRING-type global primitives, referenced by semantic typography tokens, never applied directly).

- [ ] **Step 1: Write the doc**

Create `docs/font-strategy.md` with this exact content:

```markdown
# Font strategy

The kit ships two brand typefaces, and they're distributed very differently.
This doc is the honest answer to "what font will I actually see?" for anyone
consuming the kit outside the JIT org.

## What each brand renders as

**`jit` (default brand):** `Moderat JIT`. This is a **proprietary, JIT-internal
font** — it is not bundled with the kit and has no distributable webfont.
`--font-sans` resolves it through
`var(--ds-typography-bodymd-fontfamily), ui-sans-serif, system-ui,
-apple-system, sans-serif` (`src/styles/kraken-theme.css:58`) — so most
consumers outside JIT will see the fallback stack (`ui-sans-serif` →
`system-ui` → `-apple-system` → `sans-serif`, i.e. each OS's native UI font),
**not** Moderat JIT. Only machines with the font already installed render it.

**`brand`:** `Noto Sans`, loaded via Google Fonts (`<link>` tags in
`src/app/layout.tsx`, registered under its literal name so the
`--ds-typography-bodymd-fontfamily` token resolves to it). This works for
every consumer — no fallback needed in practice.

## Supplying your own font for a new brand

Fonts are a design token like any other — a new brand supplies its own via
one Figma variable, no code change:

1. In the `semantic` collection's typography variables for your new brand's
   mode, point the font-family value at your brand's font name (see the
   `add-brand` skill, `.claude/skills/add-brand/SKILL.md`, for the full
   ritual).
2. If the font isn't OS-resident (most client fonts aren't), load it the same
   way `brand`'s Noto Sans is loaded: add a `<link>`/`@font-face` in
   `src/app/layout.tsx` (or `public/fonts/` + `@font-face` in `globals.css`
   for a self-hosted woff2 — see the note at `src/app/globals.css:17-19`).
   This is one documented example, not a new mechanism — the font pipeline
   is otherwise identical to every other token.

## Why Moderat JIT isn't bundled

It's a commercially-licensed font internal to JIT. Bundling it would require
redistribution rights the project doesn't have. The fallback chain exists
specifically so the kit still looks intentional (native OS UI font) rather
than broken (browser's generic serif) when Moderat JIT isn't present.
```

- [ ] **Step 2: Link the doc from README**

Read `README.md` around the "Creating a new theme" section (currently lines 72–86). Immediately after the closing ` ``` ` of the `npm run tokens:build` code block in that section (before the `## Quick start` heading), add:

```markdown
Fonts follow the same ritual — see [`docs/font-strategy.md`](docs/font-strategy.md)
for what each brand renders as and how a new brand supplies its own.
```

- [ ] **Step 3: Flip the status line**

In `README.md`, find the line (currently line 117):

```markdown
- ⬜ **Figma-first theming**: author themes as semantic modes in Figma; export via `token-sync` (supporting skills planned).
```

Replace it with:

```markdown
- ✅ **Figma-first theming**: author themes as semantic modes in Figma; export via `token-sync`, onboard a new brand via the `add-brand` skill.
```

- [ ] **Step 4: Verify the doc's claims against the actual source**

Run: `grep -n "font-sans:" src/styles/kraken-theme.css` — confirm it still matches the fallback chain quoted in the doc.
Run: `grep -n "Noto+Sans" src/app/layout.tsx` — confirm the Google Fonts link is still there.
Expected: both greps return a match; if either source line changed since this plan was written, update the doc's quoted text to match reality before committing.

- [ ] **Step 5: Commit**

```bash
git add docs/font-strategy.md README.md
git commit -m "docs(fonts): add font strategy doc, link from README, flip theming status to shipped"
```

---

## Orchestrator-run verification (not a subagent task)

After Task 2's runbook is committed and reviewed, the orchestrator (not a
fresh subagent) runs the live proof, using the Figma MCP access already
validated this session:

1. Add a mode named `acme` to the live `semantic` collection in JIT DS 2.1
   via `use_figma`, giving every semantic variable a visibly-different
   placeholder value (e.g. a shifted hue on colors) so the flip is
   unmistakable.
2. Hand-mirror the `acme` mode's values into a **local-only, uncommitted**
   diff of `tokens/tokens.dtcg.json` / `tokens/tokens.raw.css` (same
   mechanism as Task 1), run `npm run tokens:build`, and verify
   `data-theme="acme"` visibly re-skins the showcase app or a Storybook
   story with zero component-file changes.
3. Revert the local `tokens.dtcg.json`/`tokens.raw.css`/`tokens.css` diff
   (`git checkout -- tokens/ src/styles/tokens.css`) — this proof does not
   ship.
4. Delete the `acme` mode from the live Figma file via `use_figma`.
5. Re-run the sweep from this session's earlier retired-name check
   (`getLocalVariableCollectionsAsync` + per-variable name scan) filtered for
   `acme` instead, confirming zero residue in Figma.

This step produces no commit and touches no task's file list — it is
evidence for the milestone's definition of done, reported directly to the
user.

## Self-review notes

- **Spec coverage:** all three `docs/superpowers/specs/2026-07-13-m3-theming-design.md`
  deliverables have a task (elevation tokens → Task 1; add-brand runbook →
  Task 2 + orchestrator verification; font strategy doc → Task 3). Dark mode
  is correctly absent (dropped from scope).
- **Type consistency:** `--ds-shadow-control/overlay/modal` names are
  identical across Task 1 (creation), the add-brand skill doc (Task 2, which
  references the general ritual, not these specific tokens), and nowhere
  else redefines them.
- **No placeholders:** every JSON/CSS block above is complete, copy-paste
  content — no `TBD`, no `{ ... }` elisions. The one non-obvious value
  (`lastSyncedAt` timestamps) is a real, valid ISO timestamp, not a marker —
  Figma's own export tool would write a similarly "arbitrary but valid"
  sync time.
